{
  "name": "dzhanibekov-t-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "check:physics": "node scripts/check-conservation.mjs",
    "deploy": "npm run build && wrangler deploy",
    "preview": "npm run build && wrangler dev"
  },
  "dependencies": {
    "katex": "^0.17.0",
    "lucide": "^0.468.0",
    "three": "^0.171.0",
    "vite": "^6.0.3"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.40.2",
    "wrangler": "^4.100.0"
  }
}
